import React from "react";
import { PageTitleHeader } from "./PageTitleElements";

const PageTitle = ({ title }) => {
  return <PageTitleHeader>{title}</PageTitleHeader>;
};

export default PageTitle;
